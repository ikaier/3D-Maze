#version 330

out vec4 FragColor;

in vec2 TexCoord;

uniform sampler2D ourTexture;
uniform float ambientIntensity;
uniform vec3 lightColor;

void main()
{
	FragColor=texture(ourTexture,TexCoord)*ambientIntensity*vec4(lightColor,1.0);
}